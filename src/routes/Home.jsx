//We can use arrow function inside a (const) or a regular function
const Home = () => {
    return(
        <div>
            <h1>HOME</h1>
            <p>Home page</p>
        </div>

    )}


    export default Home;